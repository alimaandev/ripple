## Summary

<!-- What does this PR do, and why? Link the issue it closes, if any. -->

Closes #

## What changed

<!-- Bullet points of user-visible behavior changes only, if any. -->

## Testing

<!-- How did you verify it? Naming the commands you ran is enough:
     `pnpm run typecheck`, `pnpm run lint`, `pnpm run test -- --run`,
     plus any manual `ripple ...` invocations with their output summary. -->

- [ ] `pnpm run typecheck`
- [ ] `pnpm run lint` and `pnpm run format`

- [ ] `pnpm run test` passes (new tests included for changed behavior)

## Checklist

- [ ] Changelog entry added under `## [Unreleased]` when user-facing
- [ ] README or `docs/` updated when a command/flag/JSON field changed
- [ ] No secrets, no generated files (`dist/`, `coverage/`), no `*.docx`